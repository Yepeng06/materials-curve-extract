# 方案 E 设计：GOI 损失（嵌入头 + 全局正交 + 小簇合并）

> 状态：**设计稿（待用户确认后实施）**。依据：C_RESEARCH.md ChartZero（arXiv:2605.05820）
> 消融（IoU 0.75→0.82, NRMSE 0.071→0.028）、2026-08-20 最新失败诊断、本仓库代码阅读。
> 目标：压缩 >5% 桶（当前 37 条，交叉/靠近归属失败）与虚检（曲线数 168/180）。

## 一、最新失败分布（evalfix_r2 + ind + ma450，val 180 图 / 540 GT）

| 桶 | 数量 | 特征（error_pattern / 模板归因） |
|----|------|------|
| ok | 388 (71.9%) | — |
| 1-2% 边缘 | 67 | bias ≈ -1.2~-1.4% 系统偏低；log 轴 1px≈2%——**像素精度问题**（GOI 帮助有限） |
| 2-5% | 48 | low_quality_screenshot 16 / accelerated_obvious 20（退化+陡尾） |
| **>5%** | **37** | **primary_obvious 23 / marker_rich 7 / multi_curve_comparison 7——交叉/靠近归属**（GOI 主攻目标） |
| 曲线数 | 168/180 | 12 图虚检/漏检（GOI 小簇合并可再压） |

## 二、设计：在现有 K 通道 U-Net 上叠加嵌入头（最小侵入）

### 2.1 架构（train/train_segmentation_multi.py + unet.py）
- U-Net 新增**嵌入头**：`EmbedHead = Conv2d(base, E)`（E=16），挂在 dec1 输出后，与分割头并行；
  输出逐像素嵌入向量 e(p) ∈ R^16，L2 归一化。参数量 +2%，显存影响可忽略。
- 训练输出变为 (logit K 通道, embedding) 双头；**推理只取 logit 头**（嵌入仅在训练与可选的后处理合并中用）。

### 2.2 损失（GOI，ChartZero 三件套，在现有 BCE+Dice+骨架召回上叠加）
1. **类内拉近（余弦）**：
   - 每个像素的实例标签 = GT K 通道掩码的 argmax 通道；**交叉重叠像素（>1 通道激活）排除**（它们正是模型必须裁决的模糊点，拉近到任一质心都会教错）；
   - L_pull = 1 - mean(cos(e(p), c_{inst(p)}))，c_i = 该实例非重叠像素的质心嵌入（用 GT 掩码统计，stop-grad）。
2. **类间全局正交**：L_ortho = mean_{i<j} (c_i · c_j)^2（质心两两正交，全局推开不同曲线）。
3. **小簇合并（推理侧）**：通道掩码中小连通分量（面积 < max(min_area/2, 2% of 通道最大分量)）不直接丢弃——
   计算其平均嵌入，与各实例质心（置信像素 prob>0.6 统计）比余弦距离，归入最近实例；无近邻则沿用 min_area 丢弃。
4. 总损失 = BCE+Dice+0.5·SkelRecall + λ1·L_pull + λ2·L_ortho（λ1=λ2=0.1 起步，消融 0.05/0.2）。

### 2.3 实现要点
- 嵌入头初始化：小权重（避免训练初期嵌入噪声干扰分割头）；E=16 可调（消融 8/32）。
- 质心统计用 `torch.scatter` 或逐通道掩码 gather，GPU 上 ~1ms/batch。
- 骨架损失已有 GT 骨架缓存，嵌入损失直接复用 GT inst 掩码（无额外缓存）。
- **回归**：pytest（新增嵌入头形状/损失单元测试）；旧 checkpoint 兼容（load_state_dict strict=False 时嵌入头 missing 自动随机初始化——注意坑 12 的教训，续训需转移嵌入头）。

### 2.4 训练与评估计划（需用户批准后执行）
1. 本地实现 + 单测（~2h），云端 4090 训练 25 epoch（~1.5h，¥3-4）：`--init evalfix_r2.pt`（转移分割头权重，嵌入头新训）
2. 评估：val 6a/6b + 桶分布对比（目标：**>5% 桶 37→<20，6a ≥0.73**）；曲线数 168→≥172
3. 达标则跑 Phase D 500 验收；不达标回退（配置开关 goi_weight=0 即纯旧行为）

### 2.5 风险与备选
- 嵌入监督在 K=6 固定通道下是否有效（通道语义 = 曲线身份，天然适合嵌入分离）——证据：ChartZero 同源消融 +2.5pp
- 交叉重叠像素被排除在拉近外，模型仍靠分割头决策——>5% 桶若主要为「追踪跳线」而非「归属错误」，GOI 收益有限 → 备选 Phase 2 分支配对（C_PAIRING_DESIGN.md）已就绪
- 训练时间 +10%（双头前向）——可接受

## 三、决策门槛
| 指标 | 当前 | GOI 目标 |
|------|------|----------|
| 6a | 0.7185 | ≥0.73 |
| >5% 桶 | 37 条 | <20 条 |
| 曲线数 | 168/180 | ≥172 |

> 注：1-2% 边缘桶（67 条）GOI 不直接覆盖——后续候选：亚像素细化（Steger，见 C_SUBPIXEL_DESIGN.md）、focal 边缘损失，或接受现状（log 轴物理极限 1px≈2%）。

## 四、实施状态（2026-08-21）
- **代码完成**：commit 3f08b3e（嵌入头/goi_loss/segmenter embed_full/小簇合并），pytest 118/118
- **本地 5-epoch 预训练（去风险）**：训练稳定（loss 0.514→0.287；val_iou 0.689→0.756）；初步 6a/6b=0.7000/0.6910（5ep 不足），**曲线数 173/180 历史最高**
- **云端 25-epoch 完整训练**：待 4090 空闲；完成后开 multi_embed_merge: true 做 A/B，按第三节门槛验收
