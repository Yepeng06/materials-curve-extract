# 真实图鲁棒化设计：结构检测 + 刻度读取增强 + 质量分级（2026-08-26）

> 目标：应对真实材料论文图的各种常见场景；对无法提取的图**明确拒绝并给出原因**，防止输出垃圾数据。
> 背景证据：R1e 零样本诊断（54 例失败 = StructureDetectionError 34 + AxisFitError 20）、326 张蠕变库面板检测（30-60% 多面板）、
> 326 张库本地矩形轴框检测算法已验证（`dataset/panel_estimate.csv`）。
> 关联文档：`T2_DESIGN.md`（T2a/T2b/T3 已设计未实施）、`REAL_WORLD_APPLICATION_STRATEGY.md`（价值路线）、`EXPERIMENTS_BATCH1_SUMMARY.md`。

---

## 〇、实施状态（2026-08-26 晚，已提交）

| 步骤 | 状态 | 说明 |
|---|---|---|
| S1 面板检测 + 原因码框架 | ✅ 已完成 | `panel_detect.py`（矩形轴框检测，326 库实测多面板率 48.5%）+ `quality_gate.py`（A/B/C + 10 原因码）+ schema 扩展 + extractor 接入（`quality_gate` 配置开关，默认关闭，成功路径零改动）；pytest 138/138 全绿 |
| S2 T2a/T2b | ✅ 部分完成 | T2b 类别轴明确分类已实现（`is_categorical_axis` + `OCR_CATEGORICAL_AXIS`）；T2a 大部分已由 B-5a 覆盖（strict 模式保留范围外标签、y 条带 30% 宽不依赖轴列）；per-tick 4x 放大已存在（attempt 2） |
| S3 完整分级 | ✅ 核心完成 | 多面板/暗背景/刻度数/异常映射/判型低置信（AXIS_TYPE_AMBIGUOUS，`quality_min_axis_r2` 可调）均已实现；待 R1e 100 张全量分布统计 |
| S4 326 库抽样 | ⏳ 待做 | 依赖 50 张验收子集筛选（用户侧）+ paddle 评估 |
| S5 T3 面板分割 | ⏳ 待做 | 视 B 档占比决定 |

> 已知局限：面板检测依赖**闭合轴框**（matplotlib/论文常见样式）；无框样式（仅轴线）返回 0 面板，回退现有路径（保守，不会误拒）。

---

## 〇、问题精确定位（三个层，不是两个）

R1e 的 54 例失败表面分两类（结构 34 + 刻度 20），但按根因分**三层**：

| 层 | 根因 | 数量级 | 修复模块 |
|---|---|---|---|
| L1 布局层 | **多面板/多子图**：检测器把整图当一个 plot → "no vertical axis line" | ~30 | **结构检测增强**（面板检测/分割） |
| L2 结构层 | 无轴线图、双 y 轴、类别轴、y 轴无刻度线只靠标签 | ~12 | 结构检测 + 刻度读取 |
| L3 读取层 | 刻度标签过密/遮挡/上标/乱码，OCR 读不到 ≥2 个刻度 | ~12 | **刻度 OCR 增强**（per-tick + 放大 + 回退） |

> ⚠️ 注意：T2 断点调试发现部分 AxisFitError 的根因其实是 L1（多面板导致找不到主轴）——**先修 L1，L3 的修复面会缩小**。

---

## 一、模块 1：结构检测增强（`chart_structure.py`）

### 1.1 多面板检测与处理（L1 主战场）
- **面板检测**：复用已验证的矩形轴框检测算法（Otsu 二值化 → 形态学闭 → findContours → 四边形轮廓 → 面积 ≥3% 图面积 → 嵌套去重）→ 输出面板矩形列表（含置信度）；
- **处理策略（验收期）**：
  - 面板数 = 1 → 正常走现有管线；
  - 面板数 ≥ 2 → **B 档**：不自动拆（拆分会改变样本口径），返回 `STRUCT_MULTI_PANEL` 原因码 + 面板矩形坐标（供后续 T3 分割或人工确认）；
  - 面板数 = 0（无闭合轴框，如散点图/无边框图）→ 回退现有检测路径（`detect_structure` 的轴线启发式），仍失败则 C 档拒绝；
- **接入点**：`detect_structure` 开头增加 `_detect_panels()`，面板信息写入 `ChartStructure`（新增字段 `panels: List[Rect]`、`panel_count: int`）。

### 1.2 布局鲁棒（L2）
| 场景 | 现状 | 增强 |
|---|---|---|
| 双 y 轴 | 取最左轴，右轴忽略 | 检测右侧第二轴线 → 记录 `dual_y_axis: True` → B 档提示（暂不自动提取右轴数据） |
| 无轴线/散点风格 | 失败 | 边界框启发式（绘图区=非空白区域外接矩形）已有雏形，补"无轴时用图例/标题定位"回退 |
| 朝内/朝外刻度 | 朝内已支持 | 朝外刻度条带加宽（T2 条带机制扩展） |
| 旋转刻度标签 | 不支持 | 检测文本框角度（OCR 返回角度）→ 角度裁剪后再读 |

### 1.3 YOLO 兜底（可选增强，L2 长期）
- 历史 YOLO mAP50 0.942（B 系列）；补真实样式训练数据（网页图/论文图混合）后作为 `detect_structure` 失败时的第二通道；
- 优先级低（YOLO 重训成本高），先做 1.1/1.2 的经典 CV 增强。

---

## 二、模块 2：刻度读取增强（`tick_reader.py`，落地 T2_DESIGN 的 T2a/T2b）

### 2.1 T2a：y 标签中心回退（L3，论文图最常见）
- 现状：`_associate` 有"标签无匹配刻度 → 以标签中心为刻度"的回退，但 strict_y 逻辑（marks ≥4 时丢弃未匹配标签）与 y 条带裁剪区域问题使其不生效；
- 动作：
  1. y 条带裁剪检查与加宽（标签在轴左侧更外侧时，左 30% 条带不含标签 → 按文本框分布自适应条带宽度）；
  2. strict 逻辑修正：marks <4 时标签全保留（已部分存在，补验证）；
  3. 验证目标：PMC3556318 系列 3 张从 AxisFitError → OK。

### 2.2 T2b：类别轴支持（L3，ChartQA 与部分论文图）
- 刻度文本非数字但等间距 → 用索引值（0,1,2,...）映射 + 输出 `axis_kind: categorical` 注释；
- 或明确报 `OCR_CATEGORICAL_AXIS`（不崩，状态码化）——**验收期默认走"明确分类"**（曲线提取对类别轴无意义）；
- 验证目标：PMC3104219/3349504 系列从 AxisFitError → 明确分类。

### 2.3 per-tick 刻度 OCR 管线（L3 通用增强）
- 对每个候选刻度位置：按角度裁剪 + 上标位置聚类后处理 + 4× 放大重读（条带放大机制已有，扩展到 per-tick）；
- 刻度值解析加固：科学计数法/上标/Unicode 负号已有（B-5a），补"小数点噪声/单位混入"清洗；
- 验证：20 张 AxisFitError 图重跑，统计恢复率。

---

## 三、模块 3：质量分级器（A/B/C 三档 + 原因码）

### 3.1 判定信号与规则（在 `extractor.py` 编排层实现）

| 信号 | 来源 | 阈值 |
|---|---|---|
| 面板数 | 模块 1.1 | 1=A 候选；≥2=B（`STRUCT_MULTI_PANEL`）；0=回退 |
| 轴存在 | `detect_structure` | 有轴=A 候选；无轴=C（`STRUCT_NO_AXIS`） |
| 刻度数 | `read_ticks` | ≥3 有效=A；1-2=B（`OCR_FEW_TICKS`）；0=C（`OCR_NO_TICKS`） |
| OCR 平均置信度 | PaddleOCR | ≥0.8=A；0.5-0.8=B；<0.5=C（`OCR_LOW_CONF`） |
| 判型置信度 | `fit_axis` R²/残差 | 判型成功=A；模糊=B（`AXIS_TYPE_AMBIGUOUS`） |
| 类别轴 | 模块 2.2 | B（`OCR_CATEGORICAL_AXIS`，数据无意义） |
| 深底/严重退化 | 灰度直方图（白底比 <0.3） | C（`IMG_DARK_BG`） |

### 3.2 输出协议（结构化、可解释）

```
ExtractionResult 新增字段：
  status: ok | partial | rejected
  quality: "A" | "B" | "C"
  reject_code: str | None      # 原因码（见 3.3 表）
  reject_detail: str | None    # 人类可读原因 + 建议人工操作（如"请补标 2 个刻度"）
  panels: List[Rect]           # 多面板坐标（B 档提示用）
```

### 3.3 原因码表（完整枚举，测试覆盖）

| 码 | 含义 | 档位 | 建议人工操作 |
|---|---|---|---|
| `STRUCT_MULTI_PANEL` | 多面板拼图 | B | 选择面板或使用分割工具 |
| `STRUCT_NO_AXIS` | 找不到坐标轴 | C | 无 |
| `STRUCT_DUAL_Y` | 双 y 轴 | B | 指定使用哪根轴 |
| `OCR_NO_TICKS` | 刻度读取 0 个 | C | 无 |
| `OCR_FEW_TICKS` | 刻度 <3 个 | B | 人工补标刻度值 |
| `OCR_LOW_CONF` | OCR 置信度低 | B | 人工核对刻度 |
| `OCR_CATEGORICAL_AXIS` | 类别轴（非数值） | B | 确认是否需类别映射 |
| `AXIS_TYPE_AMBIGUOUS` | 线性/对数判型模糊 | B | 人工确认轴型 |
| `IMG_DARK_BG` | 深底图 | C | 无 |
| `CURVE_EXTRACT_FAIL` | 曲线提取异常 | B | 无（内部错误，报告） |

### 3.4 验证
- 回归：合成集 500+180 张全流程 **不得出现 C 档**（合成集应全 A）；
- R1e 100 张重跑：54 例失败 → 各归入 A/B/C 与原因码，统计分布（预期：~30 例 B 档多面板、~12 例 B 档类别轴/刻度、~10 例 C 档）；
- 326 张蠕变库抽 50 张（单面板优先）：统计 A/B/C 分布，作为验收基线。

---

## 四、实施顺序与验证里程碑

| 步骤 | 内容 | 验证 | 成本 |
|---|---|---|---|
| S1 | 模块 1.1 面板检测 + 原因码框架（`ChartStructure.panels`、`status/quality/reject_code` 输出协议） | 合成集回归全 A；R1e 多面板图正确报 `STRUCT_MULTI_PANEL` | 1-2 天 |
| S2 | 模块 2.1 T2a 标签回退 + 2.3 per-tick 放大 | PMC3556318 系列恢复；R1e 刻度失败数下降 | 2-3 天 |
| S3 | 模块 2.2 类别轴 + 3.1 完整分级规则 | R1e 100 张 A/B/C 分布统计；pytest 全绿 | 2-3 天 |
| S4 | 326 张蠕变库 50 张抽样评估（paddle OCR） | 验收基线报告（A/B/C 占比 + 成功图 6a/6b） | 1 天 |
| S5（可选） | T3 多面板分割（S1 后视 B 档占比决定） | 多面板图面板级提取 | 3-5 天 |

**里程碑定义**：
- M1（S1-S3 后）：真实图零样本成功率 46% → **≥65%**，垃圾输出（C 档外的不明失败）= 0；
- M2（S4 后）：50 张蠕变图 A 档 ≥70%，B 档均有可执行的人工建议；
- M3（微调后，另线）：A 档 ≥85%。

---

## 五、风险与边界（诚实声明）

1. **面板检测的误检**：曲线闭合区域/图例框可能被误判为面板（本地检测已见 17 面板异常值）→ 以"误检宁可进 B 档（提示）也不进 C 档（误拒）"为原则，面板数 ≥3 时降低置信度、要求 OCR 信号佐证；
2. **验收口径变化**：多面板图进 B 档意味着"验收成功率"分母口径要写明（按图 vs 按面板）——建议验收报告同时报"按图（A 档占比）"与"按面板（A 档占比）"；
3. **合成集回归**：所有改动必须以合成集 500+180 全 A 为硬约束（pytest + 6a/6b 回归）；
4. **真实图精度验收仍缺 gold**：A 档成功图的 RMSE/6a 仍需 50 张 gold 标注（进行中）——质量分级解决"能不能提"，gold 解决"提得准不准"，两条线并行。

---

## 六、结论

- 三层根因（布局/结构/读取）对应三个模块（面板检测、结构回退、per-tick OCR + 类别轴）；
- 质量分级器把"参差不齐"变成产品化能力：**A 档全自动、B 档可解释+最小人工介入、C 档明确拒绝**，垃圾输出率归零；
- 实施顺序 S1→S4（约 1 周），里程碑 M1：零样本成功率 46% → ≥65%，之后再靠微调冲 85%；
- 与用户真实图收集（50 张 gold）并行，S4 直接消费首批收集成果。
