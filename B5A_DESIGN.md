# B-5a 设计：OCR 刻度值误读增强（paddle 达标率 70% → 90%）

> 状态：**实施完成（A+B+C+补刻，2026-08-16）**——paddle 达标率 **70% → 86%**
> （100 张 0 硬失败；判型全部正确）。
> 范围：【无需训练】。基准：stub 不得回退；paddle eval_platform 100 张 70% → ≥90%。

## 一、实证根因（scripts/_dbg_b5a.py，12 张最差图逐刻度 OCR 文本 vs GT）

失败集中在 **log 轴 10^N 上标标签的 OCR 误读家族**（占 8/12），并非通用识别率问题：

| # | 误读类型 | 证据 | 后果 |
|---|----------|------|------|
| 1 | **'100' 双重语义**：10⁰ 上标丢失读成 '100'，与真 100 文本不可区分 | img_0056/0070/0071/0076 y 轴 GT 0.1/1 处读 '100' | 致命（0056 rmse 2437%）；值序列判型失效 |
| 2 | **'10N' 上标粘连**：10¹→'101'、10²→'102' | 8+ 张（resolve_values 已部分处理） | 软失败（'101'→101 而非 10） |
| 3 | **上标残缺**：'10-'、'012'、'0-2'、'10��' 指数读不出 | img_0038/0071/0069/0042 | 刻度丢失（3→2 个有效值） |
| 4 | **低分碎片污染**：'1'@0.107、'0'@0.519、'6'@0.392 等 | img_0020/0076 y 轴 | 虚假刻度进入序列校验 |
| 5 | **x 轴刻度标记缺失**：x_ticks_px=[] | img_0056/0066/0095 | 已走标签中心 fallback，像素有偏差 |
| 6 | 条带混入轴标题/图例文字（'Creep time (h)'、'Curve 1'、'(%)'、单字符碎片） | 多张 | parse=None 无害；碎片拆出的数字有害（同 #4） |

## 二、方案（按性价比排序，全部保留现有接口）

### A. 10^N 规范族解析 + 序列上下文消歧（核心，解决 #1/#2/#3）
- 新增 tick 值候选解析：'10'→{10, 1}、'100'→{100, 1}、'101'→{101, 10}、'10-'→{0.1}、
  '012'/'0-2'→{0.01, 12}、'10��'→{}（弃）；普通数字→{v}；
- 消歧：对整轴候选做组合选择，使判型后序列一致性得分最优（_seq_scores 复用：
  等差 lin / 等比 log 相对标准差最小）+ 与像素间距投票一致；模糊刻度 ≤3 个时穷举
  （≤8 组合），否则贪心；
- 与现有 resolve_values('10N') 合并为统一 resolve 层（勿回退原有行为：真 '100'
  在等差序列中必须保持 100）。

### B. 低分/碎片过滤（解决 #4/#6）
- score < 0.35 的框直接不进 tick（证据：真刻度 score ≥0.85，碎片 ≤0.52）；
- 条带内非纯数字长文本（轴标题/图例）不入 tick 列表（parse None 已有，加强为
  位置 + 内容双过滤：含字母 ≥2 且无数字 → 弃）。

### C. 多尺度条带 OCR 投票（解决 #3，成本最高，最后做）
- 条带 1x/2x/4x 三次识别，同框（中心距 <8px）按 rec_score×长度加权投票；
- 仅对「值序列不一致」的轴触发重试（避免全量 3x 时间开销 ~30s→60s/图）；
- 依据：PaddleOCR 社区多尺度增强实践（cloud.baidu.com/article/3693196、
  PaddleOCR Discussion #14271 limit_side_len 陷阱）+ 交接 §四.5 层1 #3。

### D. CLAHE 条带预处理（可选，验证后加）
- 低对比条带 CLAHE(clipLimit=2) 后放大；开关默认开，若 stub/达标率无回退则保留。

### E. 不做（本期）
- 竖排 y 刻度旋转识别：平台数据无竖排，留待真实图阶段（B-5，复用 B-2 CW 经验）；
- YOLO tick_label 框接入：B-4 已可给框，但本期聚焦值误读，框接入放 B-5a 后。

## 三、验证计划
1. 新增 tests/test_tick_reader_b5a.py：候选解析单测（'100'→{100,1} 等）+ 消歧
   端到端（构造含 '100' 混淆的 log 轴 → 判 log 且值正确）；
2. pytest 全绿（当前 89/89）；stub 合成/平台回归不回退；
3. 12 张诊断图复测：≥9 张恢复 ≤1%；
4. 全量 paddle eval_platform 100 张（~50min/轮）：达标率 ≥90%、轴类型 ≥98% 目标；
5. 指标汇总表写入交接文档 §3.3（B-5a 行）。

## 四、研究依据（2026-08-16 复核）
- 序列一致性错误检测 = 业界标准（US 专利 20190130614A1 [0093]）；
- 文本角色分类范式（ChartEye arXiv:2408.16123；ICDAR CHART-Infographics）；
- 多尺度投票 + CLAHE（PaddleOCR 官方实践：cloud.baidu.com/article/3693196、
  3696085；Discussion #14271）；垂直/旋转文本（cloud.baidu.com/article/3693449）；
- WebPlotDigitizer 自动刻度检测（DeepWiki 5.1）——像素间距投票佐证。

## 六、实施记录（2026-08-16）

| 步骤 | 内容 | 验证 |
|------|------|------|
| A | axis_kind.py：_candidates 候选解析（'10'/'100'/'10-'/'012'/'0-2'/'10.0'/'101' 家族）+ resolve_values 像素感知穷举消歧（单调性约束 + 每数量级像素一致性 + decade-gap 惩罚 + 10N 偏好，2-tick 平局保持原始） | 诊断案例全部正确 |
| B | tick_reader.py：tick_min_score=0.55 低分过滤（碎片 '1'@0.1 等剔除；真刻度 ≥0.71 保留） | — |
| C | tick_reader.py：值不足轴（<2 值或 2 值且 ≥4 marks）触发 4x 条带重 OCR + merge（stub 不触发） | — |
| 回归 | pytest 102/102（+13 新测试 test_tick_reader_b5a.py）；stub 合成 87.5%/平台 100%（零回退） | ✓ |
| 诊断复测 | 12 张最差 paddle 图：10/12 RECOVERED（≤1%）、2/12 IMPROVED（0056: 2437→24，0069: 0.33→0.039） | ✓ |
| 全量 | paddle eval_platform 100 张 → **86%**（0 硬失败，判型全对） | ✓ |
| 补刻 | log 轴缺刻度补全（中间 gap≈2 + 端点外推）；无回归但收益有限——剩余 14 张软失败的误差根源是刻度像素系统偏移（~2.5px）在 log 轴低端放大，信息层面不可自纠 | ✓ |

**已知限制（86% 剩余 14 张）**：全部判型正确，软失败 1-4% 来自 log 轴低端
对刻度像素系统偏移（~2.5px 检测/渲染差）的放大 + 缺刻度外推——信息层面
不可自纠。候选路径：① YOLO tick_label 框接入（B-4 已训练）提供更精确刻度
像素；② 真实图阶段（B-5）不同分布可能自动缓解；③ 需训练方案（5.6）。
