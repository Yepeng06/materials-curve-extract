# 候选方案 K：亚像素细化设计注记（2026-08-20 草稿，GOI 之后评估）

> 针对 1-2% 边缘桶（当前 67 条，占失败 44%）——GOI 不覆盖此桶。
> 依据：C_RESEARCH.md 1.3 Steger 亚像素脊线、diag_prob_peak 历史结论
> （模型概率峰值 +1~2px 系统偏移）、本会话 error_pattern（bias ~ -1.2~-1.4%）。

## 一、问题量化
- log 轴 1px ~ 2% rel → 1-2% 桶 = 0.5~1px 的系统性定位偏差
- error_pattern：失败曲线 bias 稳定在 -1.2~-1.4%（预测整体偏低）——系统偏移，可修正
- 两个疑似来源：
  1. 模型概率峰值偏移（+1~2px，训练目标/下采样导致，增强修复后待复测）
  2. _refine_chain 概率加权质心在陡峭段沿曲线方向拉长概率带 → 质心偏向（代码注释已承认）

## 二、方案（按成本排序）
| # | 方案 | 做法 | 成本 | 预期 |
|---|------|------|------|------|
| K1 | 符号化偏差校正 | 统计 val 集 per-axis-type 平均偏差（linear/log x 方向），推理统一补偿 | 极低（~30 行） | 系统性部分可消，逐图不同不彻底 |
| K2 | 质心权重修正 | _refine_chain 窗口内沿曲线法向加权（局部主成分方向投影） | 低（~40 行） | 陡段偏置消除；实现需小心 |
| K3 | Steger 脊线细化 | 通道概率图上 Hessian 脊线提取（亚像素中心），替换/辅助质心 | 中（~100 行） | 最彻底（0.1px 级），概率图需适配 |

## 三、决策门槛（GOI 评估后）
1. GOI 后重跑 diag：若 1-2% 桶仍 >=50 且 6a < 0.73 → 先做 K1 验证系统性假设
2. K1 有效（6a +0.5pp 以上）→ 上 K2；K2 不足 → K3
3. 若 GOI 已把 6a 推过 0.73，此方向降级为 Phase D 报告剩余差距记录

## 四、注意
- 亚像素细化对 6a（1% 阈值）敏感：0.5px 精度改善 = 1-2% 桶相当部分直接转 ok
- 后处理顺序：refine（亚像素）→ truncate → downsample，需回归验证不破坏现有 118 测试