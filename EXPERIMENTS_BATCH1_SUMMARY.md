# 第一批实验结果汇总（2026-08-23 技术主线）

> 范围：P1a（>5% 失败诊断）、S1（亚像素解码+偏差量化）、S2（2× 上采样验证）、S3（轴校准加固）、T1（判型信号统计）、R1（真实图数据就绪）、R1e（零样本域外诊断）、P1（接近区修复）、S1b（偏差校准）、P2（困难样本重训）、杠杆1（接近区对比损失）、杠杆2（base96 否定）、**杠杆3（链级损失，2026-08-25）**。
> 状态：**当前最优 = GOI + P1 + S1b + 杠杆1 + 杠杆3（unet_multi_chain.pt）**：val 6a **0.8481** / 6b **0.8388** / 曲线数 174/180；Phase D 500：6a **0.8641** / 曲线数 399/500。pytest 118/118 全程保持。

---

## 〇、当前最优配置（2026-08-25 杠杆3 后）

| 指标 | GOI 基线 | +P1+S1b | +杠杆1 | **+杠杆3（当前）** | 说明 |
|---|---|---|---|---|---|
| val 6a（纯召回） | 0.7352 | 0.7852 | 0.8185 | **0.8481** | +11.3pp 累计 |
| val 6b（罚虚检） | 0.7192 | 0.7681 | 0.8080 | **0.8388** | +12.0pp 累计 |
| >5% 桶 | 31 | 31 | 27 | **18** | 持续下降（31→27→25→18） |
| 2-5% 桶 | 43 | 43 | 27 | 34 | — |
| 1-2% 桶 | 69 | 39 | 44 | **29** | 链损失命中 log 轴精度 |
| 曲线数准确率 | 168/180 | 168/180 | 173/180 | **174/180** | 历史最高 |
| 训练 val_iou | 0.7740 | — | 0.7819 | **0.7938** | 历史最高 |
| **Phase D 6a** | 0.8065 | 0.8288 | 0.8582 | **0.8641** | +5.8pp 累计 |
| Phase D 曲线数 | 346/500 | 344/500 | 383/500 | **399/500** | +53 |
| 单曲线 pass≤1% | 99.4% | 99.6% | 99.6% | 99.6% | 不变 |

**杠杆3 结论**：链级损失（高斯带 L2 回归：模型逐列概率分布对齐 GT 曲线精确像素位置）——从根上训练"位置精度"，替代事后偏差校准。v1 质心 L1（背景干扰崩）、v2 band-BCE（logit 爆炸崩）、v3 排除重叠列（仍不稳）、**v4 带内 L2（梯度随 p→1 衰减，稳定）**。>5% 桶 25→18、1-2% 桶 34→29，端到端全面提升——**第二个"模型能力质变"**。

**杠杆1 结论**：接近区对比损失（hinge: 拉向正确实例质心 + 推离错误实例）+ GOI 正交权重 0.1→0.3——命中接近区嵌入无判别力根因（0.9996），>5% 桶首次下降。

**P2 结论（已存档）**：困难样本训练 val_iou 0.7754 但端到端 6a 略降——数据增强单独无效；损失设计（杠杆1/3）才是关键。

**杠杆2（base 96）否定（2026-08-25）**：base96 与 base64 checkpoint 全层 shape 不匹配（strict=False 对 shape 冲突仍 raise）→ 只能冷启动；冷启动 9 epoch val_iou 仅 0.018（收敛过慢）→ 终止。分辨率路线已被 768 实验否定——容量/分辨率杠杆在当前训练设置下不可行。

---

## 一、S3：轴校准加固（已修复并提交）✅

**发现**：`fit_axis` 的 RANSAC 离群剔除在"log 轴 + 4-5 刻度 + 刻度值污染"场景失效。
- 注入实验：随机污染一个刻度值 ×10 → **34/180 张 val 图轴映射爆炸（rel 误差 1e20+）**，全部集中在 y=log 轴。
- 根因：linear/log 两空间 RANSAC **平手时选 linear 剔除集**；污染值撞上邻居真实值（0.001×10=0.01==真实 0.01）后 linear 拟合斜率退化近 0 → pixel_to_value 除零爆炸。

**修复**（`src/mci/pipeline/coordinate_mapper.py`）：平手时按"剔除集在各自空间的拟合 R²"仲裁（linear 空间 vs log10 空间），选拟合质量高者。

**验证**：
- 注入实验：max 6e31 → **1.46**、p90 1.03 → **0.31**、爆炸 0 例
- pytest **118/118**
- 端到端回归：GOI 模型 val 180 张 recall **0.7352 完全一致**（零行为变化，180 张判型逐图一致）
- 已知限制：污染值撞真实值的极端场景仍有 ~1% 残余（21 例，rel 中位 0.3-1.5%）——记入文档，真实 OCR 撞值概率低

---

## 二、P1a：多曲线 >5% 失败诊断（结论修正了两次，最终版可信）✅

**数据**：GOI 模型 val 180 张，31 条 >5% 失败曲线逐条像素级诊断（骨架化 + 交叉结检测 + GT 曲线接近区分析 + 误差落点归属）。

**最终结论（100KB 版，判定逻辑=误差落点是否在"其他 GT 曲线"上）**：
- **crossing_jump 17 + crossing_merge 2 = 19/31（61%）归因"曲线接近/交叉区归属错误"**
- systematic 12（39%，其中多数 on_other 也高）
- **决定性证据**：
  - `frac_err_on_other_gt`：mean 0.757 / median 0.863 —— 76-86% 的误差像素落在**另一条 GT 曲线**上（预测链跟错了曲线）
  - `frac_err_in_zone`：31/31 = 1.0 —— **所有失败曲线的全部误差都发生在"曲线接近区"**（曲线间距 <5px 的区域）
  - 骨架交叉结（degree≥3）只在少数图出现——**主战场是"曲线贴近但不相交"的接近区**，不是骨架融合的交叉点

**机制**：`_trace_chain` 方向连续性贪心在接近区失效（两条曲线几乎平行、方向线索弱）+ `_refine_chain` 窗口质心把两个通道的概率混合 → 预测链在接近区切换到另一条曲线并一路跟错。

**修复方向（P1 实施）**：
1. **GOI 嵌入投票裁决接近区归属**（现有嵌入头！）：追踪时对接近区像素计算与各曲线质心嵌入的余弦相似度，选相似度高的曲线（现 `_embed_merge_masks` 只用于小簇合并，可扩展）
2. 方向连续性 + 嵌入双信号追踪
3. systematic 12 条：单独核对 pred_chan 匹配与通道整体错配

**预期收益**：>5% 桶 31 条 → 若修复 60% → 6a 从 0.7352 → ~0.775-0.80

---

## 三、S1：亚像素解码对比 + 系统性偏移量化 ✅

**单曲线 500 张（eval_phased_500_single）**：
- 三种解码（现有窗口质心 / 抛物插值 / soft-argmax T=0.25-1.0）：pass_rate 均为 99.4%、1-2% 桶均 3 条——**单曲线集上解码无差异**（该集 1-2% 桶太少，统计功效不足）
- softargmax T=0.25 的 max 略好（1.59% vs 1.77%）

**系统性偏移量化（500 张 385,808 点）**：
- 像素误差：mean 0.56px / median 0.42px / 13.1% 的点 >1px / 2.9% >2px
- **恒定偏移 +0.67px（intercept 0.674）**——模型概率峰值系统性偏移的直接证据
- log 轴含义：+0.67px ≈ **1.34% 相对误差**——直接解释 1-2% 桶

**1-2% 桶构成**（val 多曲线 69 条）：**62 条（90%）在 log 轴（x 或 y）**——与 +0.67px 偏移吻合。

**结论**：1-2% 桶的正确路径 = **经验偏差校准**（对合成集拟合"像素误差 vs 位置/线宽"校正表，推理时减去）→ log 轴误差从 1.34% 降到 ~0.7%，1-2% 桶 69 条大部分可压到 <1%。

---

## 四、S2：2× 上采样验证 ❌ 否掉

**我独立验证（500 张 2× Lanczos + 保持 512 推理）**：
- pass_rate 99.4% → **98.6%（变差）**；3 张新失败（rel 7-11%）
- 失败根因：①模型输入仍 resize 到 512（**像素当量不变**，上采样无信息增益）；②Lanczos 插值引入模糊；③放大改变刻度检测（5→4 刻度）触发**判型翻转（linear→log）**——再次印证判型对刻度完整性的依赖

**结论**：2× 上采样在 512 推理设置下不可行。真正的精度路径：偏差校准（S1）+ 训练更高分辨率模型（1024，云端，成本高，P 系列再议）。

---

## 五、T1：判型信号统计（合成集达标确认）✅

**500 张 × 2 轴 = 1000 轴**：
- **生产 fit_axis：100% 判型准确**（stub OCR 上界）——合成集 ≥98% 达标确认
- 现有三信号实际构成：值序列（权重 1.0）+ 次刻度密度（0.8）+ 轴标题 log 先验（2.0）
- 单信号错误率：S1 值序列 0/999；S4 网格线耦合 0/617（覆盖 61.7%）；**S3 次刻度密度 55.8% 错（全为 log→linear）**；纯几何恒定信号对 log 结构性失明（matplotlib log 轴 decade 像素等距）
- 推荐：**维持 fit_axis 架构**（加权投票 S1:1.0 > S3:0.8=S4:0.8 > S2:0.5-0.6 + R² 兜底），不做 veto
- 真实图补测清单：S2 标签字符（合成集零覆盖）、OCR 噪声下 S1 的 10N 重解析、网格样式多样性

---

## 六、R1：真实图数据就绪 ✅

| 源 | 数量 | 标注 | 许可 |
|---|---|---|---|
| CHART-UB（PMC 样例） | 5 张 | **完整轴/CSV 真值** | CC BY-NC-SA 3.0 |
| ChartQA | 2,565 张（29.2% 数值 x 轴≈折线类 748 张） | QA 对 + 图→表 CSV | 研究用途 |
| PMC-OA | 35 图（21 图表，12 折线/散点） | 图注 | CC BY |

- **100 张评估子集已生成**（`data/real_diag/images/eval_subset.txt`，折线主导）
- 注意：89% ChartQA 图带 alpha 通道（需合成白底）；唯一轴 GT 在 CHART-UB
- 筛选/评估脚本已就绪（estimate_chartqa_tables.py 等）

---

## 七、关键结论与下一步

### 关键结论
1. **>5% 桶主因 = 曲线接近区归属错误（61%）**——P1 已落地第一版（条件 refine），>5% 桶未动（31），下轮继续
2. **1-2% 桶主因 = +0.67px 系统性偏移（90% 在 log 轴）**——S1b 偏差校准已落地，1-2% 桶 69→39
3. **2× 上采样否掉**；**判型合成集已达标**；**轴校准已加固**（S3）
4. **真实图 gap 已量化（R1e）**：零样本成功率 46%，失败=结构检测 34 + 刻度 OCR 20（PMC 论文图全部刻度读取失败）——**真实图第一瓶颈是刻度 OCR，与合成集完全不同**
5. **GOI 嵌入对接近曲线无判别力**（质心余弦 0.9996）——嵌入裁决方案不可行，已改用概率/几何信号

### 第二批建议（按收益×成本）
| # | 任务 | 预期 | 成本 |
|---|---|---|---|
| P1b | >5% 桶：骨架融合处方向连续性增强追踪（接近区切换） | 6a +3~6pp | 中（纯后处理 2-3 天） |
| S1c | 偏差校准分桶细化（按位置/线宽/log 轴分段校准，替代恒定 -0.67） | 1-2% 桶 39→~15 | 中（1-2 天） |
| T2 | per-tick 刻度 OCR 管线（角度裁剪+上标后处理）——真实图第一瓶颈 | 真实图成功率 +20% | 中（2-3 天） |
| R2 | 渲染多样性+退化注入增强（真实图风格） | 真实图鲁棒性 | 低-中 |
| P2 | 困难样本增强（接近区重采样）| 6a 再 +2~4pp | 低（改生成器）+ 重训 |

### 产物清单
- 代码（3 commits）：S3 轴校准加固、P1 条件 refine + S1b 偏差校准、R1e 诊断脚本
- 数据：data/experiments_s3|_p1a|_s1s2|_t1|_r1e/（全部结果 JSON + 结论文档）
- 真实图池：data/real_diag/（238.8MB，MANIFEST.md + eval_subset.txt + 100 张诊断结果）
