# T2 设计：真实图刻度读取结构化修复（2026-08-23）

> 背景：R1e 零样本诊断显示真实图 54 张失败 = StructureDetectionError 34（多子图/非标准布局）+ AxisFitError 20（刻度读取失败）。断点调试结论：**管线 OCR 已能读 12/12 个 x 刻度（score>0.99），失败发生在更早的结构环节**。T2 第一层（朝内 y 刻度线检测）已落地但真实图收益为 0——失败是结构性的，需要下面的完整方案。

## 一、失败模式与根因（54 张逐类）

| 模式 | 数量 | 根因 | 对应修复 |
|---|---|---|---|
| StructureDetectionError "no vertical axis line" | ~30（ChartQA multi_col/two_col） | **多子图/多面板**：检测器把整图当一个 plot，找不到单一主轴 | T3 多子图分割 |
| AxisFitError x 轴 0-1 刻度（PMC3104219/3349504、chartqa multi_col 系列） | ~12 | x 轴是**类别轴**（非数字刻度）或标签过密 OCR 读不到 | 类别轴检测 + 放大 |
| AxisFitError y 轴 0 刻度（PMC3556318 系列） | ~6 | y 刻度线被柱状图柱子遮挡 + y 标签 OCR 失败（'P=' 乱码） | 标签中心回退 + 标签放大 |
| 其他（双 y 轴、3D、无轴线图） | ~6 | 布局超出现有检测器 | 长期（YOLO 补训） |

## 二、修复方案（按依赖顺序）

### T2a：y 标签 OCR + 标签中心回退（PMC3556318 类，2-3 天）
- 现状：`_associate` 已有"标签无匹配刻度 → 以标签中心为刻度"的回退，但**strict_y 逻辑**（marks ≥4 时丢弃未匹配标签）和 y 条带 OCR 质量导致回退不生效
- 动作：
  1. y 条带放大 4×（已有机制，验证在真实图上是否触发——attempt 1 需要刻度不够才触发，但 y 条带裁剪区域（左 30% 宽）可能不含标签）
  2. strict 逻辑修正：marks 检测数 <4 时标签全部保留（已有），但需确认 y_labels 非空——**y 条带裁剪检查**（PMC3556318 的标签 '0/50/100' 在轴左侧，条带应包含）
  3. 验证目标：PMC3556318 系列 3 张从 AxisFitError → OK

### T2b：x 类别轴支持（PMC3104219 类，2-3 天）
- 现状：`fit_axis` 要求刻度值可解析为数字（parse_number_text 失败 → value=None → 刻度不足）
- 动作：类别轴检测（x 刻度文本非数字但等间距）→ 用**索引值**（0,1,2,...）代替文本值映射，输出类别名到 data 注释；或明确报"categorical axis, not extractable"（不崩，给状态码）
- 验证目标：PMC3104219/3349504 系列从 AxisFitError → 明确分类

### T3：多子图分割（ChartQA multi_col 类，3-5 天）
- 现状：无子图分割，多面板图整体检测失败
- 动作（研究报告 T3 方案）：空白投影分割（行/列间隙）→ 每面板独立跑完整管线 → 面板标签（a/b/c）识别
- 验证目标：multi_col 系列从 StructureDetectionError → 面板级提取

## 三、优先级建议

| 优先级 | 任务 | 收益 | 成本 |
|---|---|---|---|
| 1 | **T2a 标签回退** | PMC 3 张 + 未来论文图（真实图验收依赖） | 2-3 天 |
| 2 | T2b 类别轴 | 明确分类（不崩）+ 论文图 x 轴 | 2-3 天 |
| 3 | T3 多子图 | ChartQA 30 张（但真实蠕变图多为单图，优先级低） | 3-5 天 |

**建议**：T2a 优先（直接服务"≥50 张真实蠕变图验收"——论文图 y 轴常无刻度线只靠标签）；T3 对蠕变场景收益低，可延后。T2 全程不影响合成集（合成集刻度线检测/标签均正常，pytest 回归覆盖）。

## 四、已落地（本轮）
- 朝内 y 刻度线检测（chart_structure.py，pytest 118/118，无回归）
- T2 证据脚本（diag_t2_strip_scale.py：条带放大 2×/4× 有效）
- 断点调试脚本 3 个（_dbg_t2_*）
