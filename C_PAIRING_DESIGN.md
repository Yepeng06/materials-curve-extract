# 方案 C 设计：交叉处多标签 + 骨架分支配对（2026-08-20 草案）

> 状态：**草稿（未实施）**。实施前置条件：C-2 重训（evalfix_r2）完成后复测，
> 若 local_spike 类失败（>5%）仍显著则按本设计实施；否则降级为 ablation A5 记录。
> 依据：C_RESEARCH.md §1.3 追踪/后处理、本地实验 commit 9b72238、推理代码阅读。

## 一、问题（现状代码路径）

`src/mci/pipeline/curve_extractor.py::extract_curves_multi`：

1. K 通道概率图 → **argmax 通道互斥**（`amax == c`）：每个像素只属于一个通道；
2. 阈值 0.3 → 碎片过滤 → min_area 虚检抑制 → 骨架化 → 方向连续性追踪 → 亚像素细化。

**根因**：曲线交叉/靠近处重叠区占图内墨迹 10-32%（img_0007 31.6%），
argmax 把交叉区像素全部判给一个通道 → 另一通道掩码在交叉处断口/缺段 →
追踪跳线 → **local_spike 41 条（>5% 失败）**。

**本地实验（9b72238）**：独立阈值多标签掩码平均多保留 2497px/图
（两个通道都保留交叉区像素），方向正确（LineFormer 多标签方向一致）。

## 二、文献依据（C_RESEARCH.md §1.3 摘要）

| 来源 | 做法 | 与本方案关系 |
|------|------|--------------|
| LineFormer (ICDAR'23) | 交叉处 GT 掩码多标签（Bresenham 3px 重叠）；评估 6a/6b | 多标签方向同源 |
| 骨架交叉点分支配对 | 骨架度≥3 结点=交叉 → 切分支 + Hungarian 配对（方向夹角+通道置信度+图例色距离代价）→ Dijkstra 全局重建 | 分支配对主做法 |
| EpiCurveBench | 容错序列距离配对协议 | 评估口径参考（已在 6a/6b 中） |

## 三、实施设计（两级，由简到繁，逐级验证）

### Phase 1（低风险，先做）：独立阈值多标签（去 argmax 互斥）

- 新增配置 `multi_independent_mask: true`（默认 false，可回退对比）。
- `mask01 = (region > thr)`（去掉 `& (amax == c)`）。
- 现有 `_trace_chain` 已有方向连续性：X 交叉结点处选择延续方向的分支，
  预期交叉区两条曲线各自完整通过。
- 风险与对策：
  - 交叉处骨架出现短 spur（另一曲线残留 1-3px 支线）→ 追踪前对骨架做
    spur 修剪（长度 ≤ max(3, 曲线宽度) 的悬空分支删除，方向连续性已有兜底）；
  - 虚检碎片增多 → min_area 已在位（multi_min_area），必要时微调；
  - 浅角交叉（<15°）方向判据可能选错分支 → Phase 2 兜底。
- 验证：val_multi+val_single 上 6a/6b 对比 argmax 基线；A5 ablation 记录。

### Phase 2（必要时）：交叉结点分支配对 + Hungarian

- 骨架度≥3 结点提取（skimage 邻居计数）；
- 结点处切分支（BFS 到下一结点/端点），分支特征：方向向量、长度、通道概率均值；
- 通道内分支重组：以「方向连续性（夹角余弦）+ 通道概率」为代价，
  对通道内分支做全局配对（Hungarian，成本=1-余弦 × 概率权重）；
- 若跨通道仍混淆（浅角交叉）：加入图例色距离代价（legend_matcher 已有颜色关联），
  把分支重新归属到通道；
- 输出仍为每通道一条链（顺序保证，与现有 Curve 输出兼容）。

### 回归要求（两阶段共同）

- `python -m pytest tests -q`（110/110 不回退）；
- 单曲线评估（stub 合成 ≤0.40%/87.5%、平台 100% ≤1%）不回退；
- 曲线数准确率（现 57/60）不回退；
- 6b（罚虚检口径）不得因多标签而显著恶化（虚检惩罚是 6b 的核心）。

## 四、决策门槛（重训复测后执行）

1. 复测 eval_multi_diag + diag_error_pattern：统计 local_spike（>5%）条数；
2. 若 local_spike 占比仍 >10%（相对 41 条无显著下降）→ 实施 Phase 1，
   对比 6a/6b；若 Phase 1 增益 <1pp 且失败仍存 → Phase 2；
3. 若 local_spike 已大幅下降（训练目标修复的预期收益）→ 只做 A5 ablation
   记录（argmax vs 多标签），不引入复杂度。

> 注：本设计不改变训练目标（A/B 已随重训验证）；纯推理侧后处理，
> 与 E/F/G/H 等互不冲突。
