# 合成数据主导路线的"真实应用价值"论证与提升路线（2026-08-26）

> 针对质疑："当前项目的实现由合成数据主导，但材料领域真实文献图表质量参差不齐，最后应用价值较低。"
> 证据来源：R1e 零样本域外诊断（`data/experiments_r1e/CONCLUSION.md`，100 张真实图）、326 张蠕变库面板检测（`dataset/panel_estimate.csv`）、
> 前期文献调研（`EVALUATION_METRICS_RESEARCH.md`、`材料曲线提取项目_后续工作论证报告.md`、C_RESEARCH.md）。
> 结论先行：**gap 真实存在但被系统性低估——46% 是"零样本+零适配"下限，失败 100% 集中在管线前两环节（结构检测 34 + 刻度 OCR 20），曲线提取算法本身未失败；提升路径明确（域随机化+刻度 OCR+少量微调），且"质量参差不齐"的正确产品化应对是质量分级+明确拒绝，而非追求 100% 全自动。**

---

## 一、问题量化：gap 到底在哪（先拆解，再谈价值）

### 1.1 零样本基线：46/100（46%）

| 环节 | 合成集 | 真实图零样本 | gap |
|---|---|---|---|
| 结构检测 | ~100% | ~66% 通过 | 大（34/100 失败，网页图布局非标准） |
| 刻度读取（paddle） | 100%（stub） | ~80%（有结构时） | **大（20/100 失败，论文图第一瓶颈）** |
| 判型 | 100% | 46/46 正确 | 样本少待验 |
| 曲线提取 | 6a 0.8481 | 成功图内正常 | **算法层未失败** |

**关键洞察**：54 个失败样本 **100% 死在管线前两环节**（YOLO 结构检测 + PaddleOCR 刻度读取），曲线提取算法在通过前两环节的图上工作正常。这意味着：
- gap 的修复对象**不是曲线提取模型**，而是**结构检测与刻度 OCR**——两个模块级工程问题，都有明确对策（§三）；
- 46% 是"完全零适配"的下限：没有域随机化增强、没有 per-tick OCR 管线、没有真实图微调、评估集还是最难的网页图（ChartQA 43%）+ 论文图混合。

### 1.2 "质量参差不齐"的实况（326 张蠕变库）

本地检测（`dataset/panel_estimate.csv`）：单面板仅 ~30%，**30-60% 为多面板拼图**；叠加材料论文常见退化：刻度稀疏（≤3 tick）、黑白灰度、低 DPI、扫描噪声、log 轴。**这正是"参差不齐"的量化定义**——但请注意：这些特征全部**已在合成集的退化注入覆盖范围内**（JPEG/缩放/模糊/亮度），只是**样式分布不匹配**（合成=matplotlib 模板，真实=Origin/Excel/自定义渲染）。这是"分布偏移"问题，不是"不可能"问题。

---

## 二、"质量参差不齐"的正确产品化应对：质量分级 + 明确拒绝

追求"所有真实图全自动提取"是错误目标——连人工工具（WebPlotDigitizer）都需要交互标定，最强 VLM 在真实曲线数字化上也只有 52.3% ECS（EpiCurveBench 2025）。**应用价值的正确形态是三档分级 + 失败可解释**：

| 档位 | 判定 | 系统行为 | 对应真实图比例（预估） |
|---|---|---|---|
| A 全自动 | 结构+OCR+判型全通过 | 输出曲线数据 + 置信度 | 提升后目标 70-85% |
| B 半自动兜底 | 单环节失败（如刻度 OCR 弱） | 输出"失败原因 + 建议人工操作点"（如：请补标 2 个刻度） | 10-20% |
| C 明确拒绝 | 深底/多面板/严重退化 | **明确报错，绝不输出垃圾数据** | 5-10% |

- **"不输出垃圾数据"本身就是应用价值**：材料数据库入库场景（NanoMine、Graph-FINDER 的应用场景）最大的痛点不是"提取慢"，而是"提取错的数据混入库中"——宁可拒绝，不可污染；
- 与 WebPlotDigitizer/Engauge 的差异叙事：它们需要**全程人工**标定+追踪；我们目标 **A 档全自动 + B/C 档给出可解释的失败原因与最小人工介入点**——这是"精度↑/速度↑/人工干预↓"三维对比（论证报告 §6.4 已有定位）。

---

## 三、文献证据：合成→真实的四条已验证路径

| 路径 | 证据 | 与本项目的关系 |
|---|---|---|
| **1. 域随机化增强（合成侧）** | ChartZero（arXiv:2605.05820）**纯合成 10 万张**零样本在真实图基准 ChartRM 0.921（传统管线 <0.07）；UniChart（EMNLP'23）60 万+ 合成预训练；ECharts-SynChart（IEEE 2025）多渲染引擎=域随机化 | 本项目合成器已有退化注入（JPEG/模糊/缩放），**缺的是渲染样式多样性**（字体/DPI/网格密度/灰度/面板布局/Origin-Excel 风格）——补 R2 增强即可，成本低 |
| **2. 少量真实图微调** | UniChart 合成预训练+真实微调刷新 SOTA；SAM few-shot "Cheap Lunch"；Synthesize Step-by-Step（+8.6-8.9pp） | R3 协议已定：50-100 张 gold → 60/20/20，LoRA/全量微调头——**只需 50 张**，与验收 gold 标注同一批数据 |
| **3. 混合训练** | 材料领域 Graph-FINDER（npj 2026）真实为主+多模态 | 合成 90%+真实 10% 混训，防灾难性遗忘 |
| **4. 模块级适配（结构+OCR）** | ChartOCR（WACV'21）混合框架；PP-StructureV3 方向分类；SVTR-MG（Sci Rep 2025）93.2% 优于 PP-OCRv3 | T2 per-tick 刻度 OCR 管线（角度裁剪+上标+条带放大）直接命中 20 张 AxisFitError；YOLO 补真实样式训练数据命中 34 张 StructureDetectionError |
| **反面证据（为何必须做 1-3）** | RealChart2Code（ACL'26）：纯合成训练在真实图上失效——**合成主导 ≠ 不做适配**；EpiCurveBench：最强 VLM 52.3% ECS——**通用大模型路线也救不了，专用管线+适配才是正解** | 印证：必须走"合成为基座 + 针对性适配"的混合路线 |

---

## 四、提升路线图（按成本 × 收益排序，与验收并行）

```
【现在就能做 · 低成本】
├─ T2 per-tick 刻度 OCR 管线（角度裁剪+上标+条带放大）   → 预期真实图成功率 +15-20pp（命中 AxisFitError 20 张）
├─ YOLO 结构检测增强（补真实样式/多尺度/多面板拒绝逻辑）  → 预期 +15-20pp（命中 StructureDetectionError 34 张）
├─ R2 域随机化增强（渲染多样性：字体/DPI 150-600/网格/灰度/面板/Origin-Excel 风格，重训或增量）
└─ 质量分级器（A/B/C 三档判定 + 置信度输出）             → 应用价值形态（§二）

【需 GPU/云端】
├─ 混合训练（合成 90% + 真实 10%）
└─ 真实图微调（50-100 张 gold，60/20/20，LoRA 起步）      → 预期成功率 70% → 85%+

【依赖用户真实图收集】
└─ 50 张蠕变 gold 标注（进行中）→ 双线复用：验收 + 微调 + 质量分级校准
```

**预期目标（可量化，写入验收报告）**：
- 真实图成功率：零样本 46% → 适配后 **≥70%** → 微调后 **≥85%**（A 档）；
- 失败拒绝率：C 档（垃圾输出）**= 0**——所有失败必须可解释；
- 曲线精度：gold 集上 6a/6b + nRMSE（满量程）分层报告，对标 Graph-FINDER nRMSE 0.018（≈1.8% 满量程）与人工一致性上限（ICC/CCC>0.95）。

---

## 五、应用价值度量（重新定义"价值"的可验收口径）

| 维度 | 指标 | 目标 |
|---|---|---|
| 自动化率 | A 档全自动占比（成功率） | ≥85%（微调后） |
| 数据质量 | gold 集 6a/6b、nRMSE（满量程）、ECS | 对标 Graph-FINDER/人工一致性 |
| 数据安全 | C 档明确拒绝率、垃圾输出率 | 拒绝率可解释、垃圾输出 = 0 |
| 人工介入 | B 档平均介入操作数（补几个刻度/选面板） | ≤2 次操作/张 |
| 端到端 | 单图耗时 <5s GPU（A 档） | 达标 |

**这五条才是"应用价值"的完整定义**——"成功率"单指标会低估价值（拒绝能力也是价值），"全自动"单指标会误导（人工兜底是工程常态）。

---

## 六、竞赛/论文叙事建议（把 gap 变成亮点）

1. **论文实验设计**（强说服力）：合成基座 → 域随机化 → 少量真实微调的三步消融，在真实验收集上展示逐步提升曲线 + 失败模式归因表（R1e 已给出归因方法）——"合成数据驱动的领域自适应"本身就是可发表的完整故事；
2. **答辩话术**：被问"合成数据能处理真实图吗" → 展示①零样本 46%（诚实基线）②失败 100% 归因于结构+OCR（问题定位清晰）③适配后 70-85%（可复现提升）④质量分级+拒绝机制（产品化形态）；
3. **与现有工具差异化**（论证报告 §6.4 延续）：CurveDE 需人工选点、WebPlotDigitizer 全程人工——本项目 A 档全自动 + B/C 档可解释失败，是"人工干预↓"维度的真实优势；
4. **风险声明**：合成集 6a 95% 与真实图 85% 是两条验收线（NEXT_STEPS_PROMPT 已提示），最终验收口径需与用户对齐——本报告给出"真实图 85% + 拒绝机制 + 人工兜底 ≤2 步"作为可对齐的目标形态。

---

## 七、结论

- "合成数据主导"与"真实应用价值"**不矛盾**——矛盾的是"纯合成、零适配"；文献（ChartZero/UniChart/Graph-FINDER）一致证明合成基座 + 针对性适配是图表提取领域的最优性价比路线；
- 46% 零样本基线不是天花板：失败 100% 集中在结构+OCR 两个工程环节，各有现成对策（T2/R2/YOLO 增强/微调），**预期两步提升到 70-85%**；
- "质量参差不齐"的正确答案是**质量分级 + 明确拒绝 + 最小人工兜底**，并把"拒绝能力"计入价值度量；
- 需要立刻行动的三件事：①T2 per-tick 刻度 OCR 管线；②YOLO 结构检测增强；③50 张真实图 gold（与微调/验收复用同一批数据）。
